  <send>
    <![CDATA[
      SIP/2.0 100 Trying
      [$uas_via]
      [last_Record-Route:]
      Call-ID: [call_id]
      From: <sip:<%= target.username %>@<%= target.domain %>>;tag=[pid]SIPpTag00[call_number]1234
      To: <sip:<%= sender.username %>@<%= target.domain %>>
      CSeq: <%= defined?(cseq) ? cseq : "[cseq]" %> <%= method %>
      Content-Length:  0
    ]]>
  </send>
